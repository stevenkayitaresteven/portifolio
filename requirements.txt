# --- Core (always required) --------------------------------------------------
# The policy + blur engine need only these two.
numpy>=1.26,<3
opencv-python-headless>=4.9,<5

# --- Optional extras (install only what you use) -----------------------------
# Box-level nudity detector — offline ONNX model ships inside the wheel:
#     pip install nudenet onnxruntime
#
# Hugging Face multimodal backends — toxic-bert (text), Falconsai NSFW ViT
# (image), Whisper ASR (audio); non-WAV audio and video soundtracks also need
# the `ffmpeg` binary on PATH:
#     pip install "transformers>=4.45" torch pillow
#
# Chat UI / browser UI / HTTP service (safety/chat.py, safety/ui.py, safety/service.py):
#     pip install fastapi "uvicorn[standard]" python-multipart
#
# Fine-tuning a custom classifier (training/export only — see safety/train/):
#     pip install torch torchvision pillow onnx
#
# Or install everything via the package extras (see pyproject.toml):
#     pip install -e ".[detectors,hf,serve,train]"
